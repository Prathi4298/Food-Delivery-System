import React from 'react';

const Orders = () => {
    return (
        <div>
            <h1>My Orders</h1>
            {/* Display user's orders */}
        </div>
    );
};

export default Orders;
